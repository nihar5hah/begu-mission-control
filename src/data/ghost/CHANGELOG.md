# CHANGELOG

## [2026-02-10] - Session Archived
### Added
- **Project Ghost**: Initialized the session synchronization and automatic dev logging system (`ghost.py`).
- **Google Workspace Tips**: Updated `TOOLS.md` with critical usage notes for the `gog` CLI:
  - Clarified that `gog calendar events list` is interpreted incorrectly; use `gog calendar events primary` or `gog calendar list` instead.
  - Noted the importance of the `--account` flag for predictable results.

### Fixed
- **Begu Mission Control**: Synchronized subproject state to latest commit (`50493e51`).
